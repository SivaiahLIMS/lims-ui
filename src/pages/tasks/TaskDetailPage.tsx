import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function TaskDetailPage() {
  return <PageWrapper title="Task Detail" breadcrumbs={[{ label: 'Task Detail' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
