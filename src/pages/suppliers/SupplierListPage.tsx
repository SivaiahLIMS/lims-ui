import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function SupplierListPage() {
  return <PageWrapper title="Supplier List" breadcrumbs={[{ label: 'Supplier List' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
