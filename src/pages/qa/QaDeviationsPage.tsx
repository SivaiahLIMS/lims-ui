import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function QaDeviationsPage() {
  return <PageWrapper title="Qa Deviations" breadcrumbs={[{ label: 'Qa Deviations' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
