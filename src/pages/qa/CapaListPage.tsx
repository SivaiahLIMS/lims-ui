import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function CapaListPage() {
  return <PageWrapper title="Capa List" breadcrumbs={[{ label: 'Capa List' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
