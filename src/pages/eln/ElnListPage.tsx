import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function ElnListPage() {
  return <PageWrapper title="Eln List" breadcrumbs={[{ label: 'Eln List' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
