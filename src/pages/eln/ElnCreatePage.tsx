import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function ElnCreatePage() {
  return <PageWrapper title="Eln Create" breadcrumbs={[{ label: 'Eln Create' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
