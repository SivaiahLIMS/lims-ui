import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function NotificationsPage() {
  return <PageWrapper title="Notifications" breadcrumbs={[{ label: 'Notifications' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
