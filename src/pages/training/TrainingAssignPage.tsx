import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function TrainingAssignPage() {
  return <PageWrapper title="Training Assign" breadcrumbs={[{ label: 'Training Assign' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
