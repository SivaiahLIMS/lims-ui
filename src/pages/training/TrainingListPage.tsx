import { Typography } from '@mui/material';
import { PageWrapper } from '@/components/layout/PageWrapper';
export default function TrainingListPage() {
  return <PageWrapper title="Training List" breadcrumbs={[{ label: 'Training List' }]}><Typography color="text.secondary">Module under construction.</Typography></PageWrapper>;
}
